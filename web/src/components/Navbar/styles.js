import theme from "../../assets/theme/light";

const styles = {
  container: {
    height: '10vh',
    backgroundColor: theme.color.main1,
    boxShadow: `2px 2px 4px ${theme.color.shadow}`,
    display: 'flex',
    alignItems: 'center',
    paddingLeft: theme.spacing.xxxl,
    paddingRight: theme.spacing.xxxl,
  }
};

export default styles;